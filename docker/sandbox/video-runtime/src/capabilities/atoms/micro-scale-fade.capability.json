{
  "id": "video.component.micro-scale-fade",
  "kind": "component",
  "summary": "A restrained scale settle gives supporting text a quiet entrance.",
  "category": "typography",
  "tags": ["text", "entrance", "scale", "subtle"],
  "vibe": "clean",
  "use_for": ["Labels and supporting headings", "Quiet secondary text entrances"],
  "avoid_for": ["Hero kinetic moments", "Per-character reveals", "Text exits"],
  "natural_frame_length": 60,
  "tier": "vetted",
  "dependencies": ["remotion"],
  "native_canvas": {"width": 1280, "height": 720, "fps": 30},
  "deterministic": true,
  "test_props": {"text": "Quietly polished.", "scaleFrom": 0.96},
  "props_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["text"],
    "properties": {
      "text": {"type": "string", "minLength": 1, "maxLength": 160},
      "scaleFrom": {"type": "number", "minimum": 0.5, "maximum": 1.5},
      "fontSize": {"type": "number", "minimum": 16, "maximum": 180},
      "color": {"type": "string", "minLength": 1, "maxLength": 64},
      "fontWeight": {"type": "number", "minimum": 100, "maximum": 900},
      "speed": {"type": "number", "minimum": 0.25, "maximum": 4}
    }
  },
  "upstream_docs_url": "https://remocn.dev/docs/typography/micro-scale-fade.md",
  "vendored_revision": "registry-fetched-2026-08-28",
  "loader": "component.micro-scale-fade"
}
