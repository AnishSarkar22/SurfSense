{
  "id": "video.component.per-character-rise",
  "kind": "component",
  "summary": "Characters rise into place with a crisp, even stagger.",
  "category": "typography",
  "tags": ["text", "characters", "stagger", "entrance", "vertical"],
  "vibe": "clean",
  "use_for": ["Short kinetic phrases", "Precise character-by-character entrances"],
  "avoid_for": ["Paragraphs", "Line-level reveals", "Blurred entrances"],
  "natural_frame_length": 60,
  "tier": "vetted",
  "dependencies": ["remotion"],
  "native_canvas": {"width": 1280, "height": 720, "fps": 30},
  "deterministic": true,
  "test_props": {"text": "One more thing.", "distance": 32},
  "props_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["text"],
    "properties": {
      "text": {"type": "string", "minLength": 1, "maxLength": 80},
      "distance": {"type": "number", "minimum": 0, "maximum": 120},
      "fontSize": {"type": "number", "minimum": 16, "maximum": 180},
      "color": {"type": "string", "minLength": 1, "maxLength": 64},
      "fontWeight": {"type": "number", "minimum": 100, "maximum": 900},
      "speed": {"type": "number", "minimum": 0.25, "maximum": 4}
    }
  },
  "upstream_docs_url": "https://remocn.dev/docs/typography/per-character-rise.md",
  "vendored_revision": "registry-fetched-2026-08-28",
  "loader": "component.per-character-rise"
}
