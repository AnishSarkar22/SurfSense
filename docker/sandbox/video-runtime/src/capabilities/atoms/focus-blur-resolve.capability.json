{
  "id": "video.component.focus-blur-resolve",
  "kind": "component",
  "summary": "A short phrase pulls into crisp focus before softly blurring away.",
  "category": "typography",
  "tags": ["text", "blur", "focus", "entrance", "exit"],
  "vibe": "premium",
  "use_for": ["Cinematic hero phrases", "Self-contained focus-in and focus-out arcs"],
  "avoid_for": ["Paragraphs", "Sharp frame-one entrances", "Entrance-only reveals"],
  "natural_frame_length": 90,
  "tier": "vetted",
  "dependencies": ["remotion"],
  "native_canvas": {"width": 1280, "height": 720, "fps": 30},
  "deterministic": true,
  "test_props": {"text": "Focus resolves clearly.", "blur": 14},
  "props_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["text"],
    "properties": {
      "text": {"type": "string", "minLength": 1, "maxLength": 120},
      "blur": {"type": "number", "minimum": 0, "maximum": 40},
      "fontSize": {"type": "number", "minimum": 16, "maximum": 180},
      "color": {"type": "string", "minLength": 1, "maxLength": 64},
      "fontWeight": {"type": "number", "minimum": 100, "maximum": 900},
      "speed": {"type": "number", "minimum": 0.25, "maximum": 4}
    }
  },
  "upstream_docs_url": "https://remocn.dev/docs/typography/focus-blur-resolve.md",
  "vendored_revision": "registry-fetched-2026-08-28",
  "loader": "component.focus-blur-resolve"
}
