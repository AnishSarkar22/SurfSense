{
  "id": "video.component.animated-bar-chart",
  "kind": "component",
  "summary": "Bars spring from the baseline in a staggered cascade.",
  "category": "data-visualization",
  "tags": ["chart", "bar", "categorical", "metrics"],
  "vibe": "data",
  "use_for": ["Comparative categorical metrics", "KPI result reveals"],
  "avoid_for": ["Time series", "Single values"],
  "natural_frame_length": 90,
  "tier": "vetted",
  "dependencies": ["remotion"],
  "native_canvas": {"width": 1280, "height": 720, "fps": 30},
  "deterministic": true,
  "test_props": {"data": [35, 60, 45, 80], "labels": ["Q1", "Q2", "Q3", "Q4"]},
  "props_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["data"],
    "properties": {
      "data": {"type": "array", "minItems": 2, "maxItems": 20, "items": {"type": "number", "minimum": 0}},
      "labels": {"type": "array", "maxItems": 20, "items": {"type": "string", "maxLength": 24}},
      "width": {"type": "number", "minimum": 100, "maximum": 1280},
      "height": {"type": "number", "minimum": 100, "maximum": 720},
      "barColor": {"type": "string", "minLength": 1},
      "gap": {"type": "number", "minimum": 0, "maximum": 100},
      "staggerFrames": {"type": "number", "minimum": 0, "maximum": 30}
    }
  },
  "upstream_docs_url": "https://remocn.dev/docs/ui-blocks/animated-bar-chart.md",
  "vendored_revision": "registry-2026-08-25",
  "loader": "component.animated-bar-chart"
}
