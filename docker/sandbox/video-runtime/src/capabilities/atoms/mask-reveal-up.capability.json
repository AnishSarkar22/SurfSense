{
  "id": "video.component.mask-reveal-up",
  "kind": "component",
  "summary": "Newline-delimited text rises line by line through a soft mask.",
  "category": "typography",
  "tags": ["text", "lines", "reveal", "mask", "vertical"],
  "vibe": "clean",
  "use_for": ["Multi-line headlines", "Stacked text reveals"],
  "avoid_for": ["Character-level reveals", "Long paragraphs", "Lateral motion"],
  "natural_frame_length": 90,
  "tier": "vetted",
  "dependencies": ["remotion"],
  "native_canvas": {"width": 1280, "height": 720, "fps": 30},
  "deterministic": true,
  "test_props": {"text": "Designed to move.\nBuilt to focus.", "distance": 30},
  "props_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["text"],
    "properties": {
      "text": {"type": "string", "minLength": 1, "maxLength": 240},
      "distance": {"type": "number", "minimum": 0, "maximum": 120},
      "fontSize": {"type": "number", "minimum": 16, "maximum": 180},
      "color": {"type": "string", "minLength": 1, "maxLength": 64},
      "fontWeight": {"type": "number", "minimum": 100, "maximum": 900},
      "speed": {"type": "number", "minimum": 0.25, "maximum": 4}
    }
  },
  "upstream_docs_url": "https://remocn.dev/docs/typography/mask-reveal-up.md",
  "vendored_revision": "registry-fetched-2026-08-28",
  "loader": "component.mask-reveal-up"
}
