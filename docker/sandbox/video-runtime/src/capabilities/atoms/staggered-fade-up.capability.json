{
  "id": "video.component.staggered-fade-up",
  "kind": "component",
  "summary": "Words fade and rise sequentially in a restrained cascade.",
  "category": "typography",
  "tags": ["text", "words", "stagger", "fade", "entrance"],
  "vibe": "clean",
  "use_for": ["Longer headlines", "Neutral word-by-word reveals"],
  "avoid_for": ["One-word titles", "Character-level reveals", "Text exits"],
  "natural_frame_length": 90,
  "tier": "vetted",
  "dependencies": ["remotion"],
  "native_canvas": {"width": 1280, "height": 720, "fps": 30},
  "deterministic": true,
  "test_props": {"text": "Ship videos faster", "staggerDelay": 4},
  "props_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["text"],
    "properties": {
      "text": {"type": "string", "minLength": 1, "maxLength": 200},
      "staggerDelay": {"type": "number", "minimum": 0, "maximum": 20},
      "distance": {"type": "number", "minimum": 0, "maximum": 120},
      "fontSize": {"type": "number", "minimum": 16, "maximum": 180},
      "color": {"type": "string", "minLength": 1, "maxLength": 64},
      "fontWeight": {"type": "number", "minimum": 100, "maximum": 900},
      "speed": {"type": "number", "minimum": 0.25, "maximum": 4}
    }
  },
  "upstream_docs_url": "https://remocn.dev/docs/typography/staggered-fade-up.md",
  "vendored_revision": "registry-fetched-2026-08-28",
  "loader": "component.staggered-fade-up"
}
